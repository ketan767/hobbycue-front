import { withAuth } from '@/navigation/withAuth'
import React from 'react'
// import styles from ''

type Props = {}

const Explore: React.FC<Props> = (props) => {
  return <>Explore to be done!</>
}

export default withAuth(Explore)
