import { withAuth } from '@/navigation/withAuth'
import React from 'react'
// import styles from ''

type Props = {}

const Community: React.FC<Props> = (props) => {
  return <>Community to be done!</>
}

export default withAuth(Community)
